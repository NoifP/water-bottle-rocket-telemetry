#pragma once
// Local overrides -- copy this file to config_local.h and customise.
// config_local.h is gitignored and will not be committed to the repo.

// Override the rocket MAC address (get this from the rocket's boot log)
#undef ROCKET_MAC
static const uint8_t ROCKET_MAC[] = {0xAA, 0xBB, 0xCC, 0xDD, 0xEE, 0xFF};
